<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.studyboot.sms.dao.RateRequireDao">

 <!-- sms_member_rate_info 테이블 -->
  <resultMap type="RateRequire" id="rateRequireMap">
    <id column="std_id" property="studyNo" />
    <id column="member_id" property="memberNo" />
    <result column="rate_require" property="rateRequire" />
    <result column="std_member_cnt" property="studyMemberCount" />
    <result column="nick_name" property="nickName" />
    <result column="photo" property="photo" />
    <result column="name" property="name" />
  </resultMap>

  <insert id="rateRequire" parameterType="map">
    insert into  sms_member_retire 
      values(
      #{studyNo},
      #{memberNo},
      #{rateRequire},
      #{memberCount}
      )
  </insert>
  
  <!-- 모든 스터디 멤버에게 평가받지 못한 탈퇴자들 뽑음 -->
  <select id="retireTrueOrFalse"  resultMap="rateRequireMap" parameterType="map">
    select 
      smr.std_id,
      smr.member_id,
      smr.rate_require,
      sm.nick_name,
      sm.photo,
      sm.name
    from
     sms_member_retire smr
      inner join sms_std_member ssm on ssm.member_id = smr.member_id and smr.std_id = ssm.std_id
      inner join sms_member sm on sm.member_id = ssm.member_id
    where smr.std_id = #{studyNo} and smr.rate_require = #{rateRequire}
  </select> 
  
  
  <select id="retireEvaluationWait" resultMap="rateRequireMap" parameterType="map">
    select 
      smr.std_id,
      smr.member_id,
      smr.rate_require,
      sm.nick_name,
      sm.photo,
      sm.name
    from
     sms_member_retire smr
      inner join sms_std_member ssm on ssm.member_id = smr.member_id and smr.std_id = ssm.std_id
      inner join sms_member sm on sm.member_id = ssm.member_id
    where smr.std_id = #{studyNo} and smr.rate_require = #{rateRequire} and smr.member_id = #{confirmNo}
  </select>

  <insert id="retireeEvaluation" parameterType="map">
    insert into  sms_member_rate_info(
      std_id, member_id, confirm_member_id, rate, rate_dt
      )
    values(
    #{studyNo},
    #{memberNo},
    #{confirmMember},
    #{rate},
    #{rateDate}
    )
  </insert>
</mapper>






